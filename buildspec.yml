version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install
  build:
    commands:
      - npm test
      - echo "Build Selesai"
artifacts:
  files:
    - '**/*'